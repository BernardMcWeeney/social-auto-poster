{
  "name": "social-oauth-broker",
  "version": "1.0.0",
  "description": "Cloudflare Worker OAuth broker for Greenberry Social WordPress plugin",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
